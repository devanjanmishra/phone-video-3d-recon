# Example outputs

Replace the placeholders referenced by the root README with real renders:

| File | What to capture |
|---|---|
| `01_fused_cloud.png` | `recon/fused_points.ply` in Open3D / MeshLab / CloudCompare |
| `02_planes_colored.png` | `cad/planes_colored.ply` — the per-plane colouring is the selling point |
| `03_floor_plan_dxf.png` | `cad/house_plan.dxf` opened in FreeCAD, with dimensions annotated |

Keep each under ~500 KB. `planes.json` from the same capture can be committed
alongside as a concrete sample of the CAD output.
